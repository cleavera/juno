import { Combination } from './combination';

export class Comparison {
  public combinations: Array<Combination>;

  constructor(combinations: Array<Combination>) {
    this.combinations = combinations;
  }
}
